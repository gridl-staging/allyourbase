import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"guide/security.md","filePath":"guide/security.md"}');
const _sfc_main = { name: "guide/security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h1><p>This guide documents security features currently shipped in AYB, based on code in <code>internal/vault</code>, <code>internal/auth</code>, and <code>internal/server</code>.</p><h2 id="encryption-at-rest" tabindex="-1">Encryption at rest <a class="header-anchor" href="#encryption-at-rest" aria-label="Permalink to &quot;Encryption at rest&quot;">​</a></h2><p>AYB secrets are encrypted at rest through the vault subsystem.</p><h3 id="vault-encryption-model" tabindex="-1">Vault encryption model <a class="header-anchor" href="#vault-encryption-model" aria-label="Permalink to &quot;Vault encryption model&quot;">​</a></h3><p>From <code>internal/vault/vault.go</code>:</p><ul><li>Encryption algorithm: <strong>AES-256-GCM</strong>.</li><li>Key derivation: <strong>HKDF-SHA256</strong> per secret.</li><li>Per-secret randomness: each encrypted secret gets a random HKDF salt and GCM nonce.</li><li>Stored values: ciphertext + nonce payload (salt + nonce).</li></ul><h3 id="master-key-resolution-order" tabindex="-1">Master key resolution order <a class="header-anchor" href="#master-key-resolution-order" aria-label="Permalink to &quot;Master key resolution order&quot;">​</a></h3><p><code>ResolveMasterKey</code> uses this precedence order:</p><ol><li><code>AYB_VAULT_MASTER_KEY</code> environment variable.</li><li><code>vault.master_key</code> config value.</li><li>Persisted key file at <code>~/.ayb/vault-key</code>.</li><li>If none exist, generate a new random 32-byte key and persist it to <code>~/.ayb/vault-key</code>.</li></ol><h3 id="secret-storage-behavior" tabindex="-1">Secret storage behavior <a class="header-anchor" href="#secret-storage-behavior" aria-label="Permalink to &quot;Secret storage behavior&quot;">​</a></h3><p>From <code>internal/vault/store.go</code>:</p><ul><li>Secrets are persisted encrypted in Postgres (<code>_ayb_vault_secrets</code>).</li><li><code>CreateSecret</code> fails on duplicates.</li><li><code>SetSecret</code> is an upsert (create-or-update).</li><li><code>UpdateSecret</code> and <code>DeleteSecret</code> return not-found when the secret does not exist.</li><li><code>ListSecrets</code> returns metadata only (name/timestamps), not plaintext values.</li></ul><h2 id="audit-logs" tabindex="-1">Audit logs <a class="header-anchor" href="#audit-logs" aria-label="Permalink to &quot;Audit logs&quot;">​</a></h2><h3 id="platform-audit-log" tabindex="-1">Platform audit log <a class="header-anchor" href="#platform-audit-log" aria-label="Permalink to &quot;Platform audit log&quot;">​</a></h3><ul><li><code>GET /api/admin/audit</code></li></ul><p>Supported filters include:</p><ul><li><code>table</code></li><li><code>user_id</code></li><li><code>operation</code> (<code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code>)</li><li><code>from</code>, <code>to</code></li><li><code>limit</code>, <code>offset</code></li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;http://localhost:8090/api/admin/audit?table=orders&amp;operation=UPDATE&amp;limit=50&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -H</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;Authorization: Bearer </span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$AYB_ADMIN_TOKEN</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span></span></code></pre></div><h3 id="tenant-and-org-audit-views" tabindex="-1">Tenant and org audit views <a class="header-anchor" href="#tenant-and-org-audit-views" aria-label="Permalink to &quot;Tenant and org audit views&quot;">​</a></h3><ul><li>Tenant audit: <code>GET /api/admin/tenants/{tenantId}/audit</code></li><li>Organization audit: <code>GET /api/admin/orgs/{orgId}/audit</code></li></ul><h2 id="rate-limiting" tabindex="-1">Rate limiting <a class="header-anchor" href="#rate-limiting" aria-label="Permalink to &quot;Rate limiting&quot;">​</a></h2><p>AYB uses sliding-window in-memory limiters in <code>internal/auth/ratelimit.go</code> and <code>internal/auth/app_ratelimit.go</code>.</p><h3 id="ip-based-limiter" tabindex="-1">IP-based limiter <a class="header-anchor" href="#ip-based-limiter" aria-label="Permalink to &quot;IP-based limiter&quot;">​</a></h3><p>The core limiter sets these headers on responses:</p><ul><li><code>X-RateLimit-Limit</code></li><li><code>X-RateLimit-Remaining</code></li><li><code>X-RateLimit-Reset</code></li><li><code>Retry-After</code> (on 429 only)</li></ul><p>A denied request returns HTTP <code>429</code>.</p><h3 id="app-based-limiter" tabindex="-1">App-based limiter <a class="header-anchor" href="#app-based-limiter" aria-label="Permalink to &quot;App-based limiter&quot;">​</a></h3><p>When an authenticated request has app-scoped claims with configured app RPS limits, AYB applies per-app limits and sets:</p><ul><li><code>X-App-RateLimit-Limit</code></li><li><code>X-App-RateLimit-Remaining</code></li><li><code>X-App-RateLimit-Reset</code></li><li><code>Retry-After</code> (on 429 only)</li></ul><h3 id="config-knobs" tabindex="-1">Config knobs <a class="header-anchor" href="#config-knobs" aria-label="Permalink to &quot;Config knobs&quot;">​</a></h3><p>Server startup wires these limiters from config (<code>server_init.go</code>, <code>routes_auth.go</code>):</p><ul><li><code>auth.rate_limit</code> (general auth limiter)</li><li><code>auth.anonymous_rate_limit</code> (anonymous auth traffic)</li><li><code>auth.rate_limit_auth</code> (sensitive auth routes)</li><li><code>rate_limit.api</code> (authenticated API traffic)</li><li><code>rate_limit.api_anonymous</code> (anonymous API traffic)</li><li><code>admin.login_rate_limit</code> (admin login endpoint)</li></ul><p>Config value types differ by key:</p><ul><li><code>auth.rate_limit</code> and <code>admin.login_rate_limit</code> are integer &quot;requests per minute&quot; values.</li><li><code>auth.rate_limit_auth</code>, <code>rate_limit.api</code>, and <code>rate_limit.api_anonymous</code> use rate-limit spec strings parsed as <code>N/min</code> or <code>N/hour</code>.</li></ul><h2 id="api-key-scopes" tabindex="-1">API key scopes <a class="header-anchor" href="#api-key-scopes" aria-label="Permalink to &quot;API key scopes&quot;">​</a></h2><p>API keys are implemented in <code>internal/auth/apikeys.go</code> and enforced by middleware plus <code>ResolveAPIKeyTenantAccess</code>.</p><h3 id="key-format-and-lifecycle" tabindex="-1">Key format and lifecycle <a class="header-anchor" href="#key-format-and-lifecycle" aria-label="Permalink to &quot;Key format and lifecycle&quot;">​</a></h3><ul><li>Prefix: all API keys begin with <code>ayb_</code> (<code>APIKeyPrefix</code>).</li><li>Keys are generated as random bytes and stored hashed.</li><li>Revoked keys are rejected (<code>ErrAPIKeyRevoked</code>).</li><li>Expired keys are rejected if <code>expires_at</code> is set and in the past (<code>ErrAPIKeyExpired</code>).</li></ul><h3 id="scope-values" tabindex="-1">Scope values <a class="header-anchor" href="#scope-values" aria-label="Permalink to &quot;Scope values&quot;">​</a></h3><p>Allowed scope strings (<code>internal/auth/auth.go</code>):</p><ul><li><code>*</code> (full access)</li><li><code>readonly</code></li><li><code>readwrite</code></li></ul><h3 id="three-scoping-patterns" tabindex="-1">Three scoping patterns <a class="header-anchor" href="#three-scoping-patterns" aria-label="Permalink to &quot;Three scoping patterns&quot;">​</a></h3><p><code>CreateAPIKeyOptions</code> supports three patterns:</p><ul><li><strong>User-scoped key</strong>: no <code>appId</code>, no <code>orgId</code> (legacy/default behavior).</li><li><strong>App-scoped key</strong>: <code>appId</code> set; app rate-limit claims may be attached.</li><li><strong>Org-scoped key</strong>: <code>orgId</code> set; access is limited to tenants belonging to that org.</li></ul><p><code>appId</code> and <code>orgId</code> are mutually exclusive.</p><h3 id="table-restrictions" tabindex="-1">Table restrictions <a class="header-anchor" href="#table-restrictions" aria-label="Permalink to &quot;Table restrictions&quot;">​</a></h3><p><code>allowedTables</code> restricts table access. Empty means all tables are allowed.</p><h3 id="org-scope-enforcement" tabindex="-1">Org-scope enforcement <a class="header-anchor" href="#org-scope-enforcement" aria-label="Permalink to &quot;Org-scope enforcement&quot;">​</a></h3><p>For org-scoped keys, AYB checks tenant context at request time (<code>routes_api.go</code>, <code>tenant_middleware.go</code>):</p><ul><li>If tenant org matches key org, request continues.</li><li>If tenant org does not match, request is rejected with HTTP <code>403</code>.</li></ul><h2 id="secret-name-validation" tabindex="-1">Secret name validation <a class="header-anchor" href="#secret-name-validation" aria-label="Permalink to &quot;Secret name validation&quot;">​</a></h2><p>From <code>internal/vault/secret_name.go</code>:</p><ul><li>Allowed characters: <code>[A-Za-z0-9_.-]</code> (letters, digits, underscore, hyphen, dot).</li><li>Names cannot contain <code>..</code> (path traversal prevention).</li><li>Names are trimmed of surrounding whitespace before validation.</li><li>Empty names are rejected.</li></ul><h2 id="transport-and-response-hardening" tabindex="-1">Transport and response hardening <a class="header-anchor" href="#transport-and-response-hardening" aria-label="Permalink to &quot;Transport and response hardening&quot;">​</a></h2><p>From <code>internal/server/middleware.go</code>:</p><ul><li>CORS is explicitly configured (<code>Access-Control-Allow-*</code> headers).</li><li>Security response headers are always set: <ul><li><code>X-Content-Type-Options: nosniff</code></li><li><code>X-Frame-Options: DENY</code></li><li><code>Referrer-Policy: strict-origin-when-cross-origin</code></li></ul></li><li>AYB does not currently set CSP, HSTS, or other browser hardening headers in this middleware path.</li></ul><h2 id="secrets-management-endpoints" tabindex="-1">Secrets management endpoints <a class="header-anchor" href="#secrets-management-endpoints" aria-label="Permalink to &quot;Secrets management endpoints&quot;">​</a></h2><p>Admin secrets endpoints:</p><ul><li><code>GET /api/admin/secrets</code></li><li><code>GET /api/admin/secrets/{name}</code></li><li><code>POST /api/admin/secrets</code></li><li><code>PUT /api/admin/secrets/{name}</code></li><li><code>DELETE /api/admin/secrets/{name}</code></li><li><code>POST /api/admin/secrets/rotate</code> (JWT secret rotation)</li></ul><p><code>POST /api/admin/secrets/rotate</code> is only registered when the auth service is configured.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -X</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> POST</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> http://localhost:8090/api/admin/secrets</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -H</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;Authorization: Bearer </span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$AYB_ADMIN_TOKEN</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -H</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;Content-Type: application/json&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{&quot;name&quot;:&quot;SMTP_PASSWORD&quot;,&quot;value&quot;:&quot;&lt;secret&gt;&quot;}&#39;</span></span></code></pre></div><h2 id="related-guides" tabindex="-1">Related guides <a class="header-anchor" href="#related-guides" aria-label="Permalink to &quot;Related guides&quot;">​</a></h2><ul><li><a href="/guide/organizations.html">Organizations</a></li><li><a href="/guide/admin-dashboard.html">Admin Dashboard</a></li><li><a href="/guide/authentication.html">Authentication</a></li><li><a href="/guide/saml.html">SAML SSO</a></li><li><a href="/guide/custom-domains.html">Custom Domains</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const security = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  security as default
};
