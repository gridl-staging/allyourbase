import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Custom Domains","description":"","frontmatter":{},"headers":[],"relativePath":"guide/custom-domains.md","filePath":"guide/custom-domains.md"}');
const _sfc_main = { name: "guide/custom-domains.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="custom-domains" tabindex="-1">Custom Domains <a class="header-anchor" href="#custom-domains" aria-label="Permalink to &quot;Custom Domains&quot;">​</a></h1><p>This guide covers AYB&#39;s current custom-domain lifecycle: admin API setup, DNS ownership verification, certificate provisioning/renewal, route-table behavior, and status transitions.</p><p>Source of truth:</p><ul><li><code>internal/server/custom_domains.go</code></li><li><code>internal/server/custom_domains_handler.go</code></li><li><code>internal/server/custom_domains_store.go</code></li><li><code>internal/server/custom_domains_verify.go</code></li><li><code>internal/server/custom_domains_cert.go</code></li><li><code>internal/server/custom_domains_health.go</code></li><li><code>internal/server/custom_domains_route.go</code></li><li>Tests: <code>custom_domains_handler_test.go</code>, <code>custom_domains_verify_test.go</code>, <code>custom_domains_cert_test.go</code>, <code>custom_domains_health_test.go</code>, <code>custom_domains_route_test.go</code></li></ul><p>Persistence is in <code>_ayb_custom_domains</code>.</p><h2 id="lifecycle-prerequisites" tabindex="-1">Lifecycle prerequisites <a class="header-anchor" href="#lifecycle-prerequisites" aria-label="Permalink to &quot;Lifecycle prerequisites&quot;">​</a></h2><ul><li>Enable the jobs service/scheduler so AYB can register domain verification, route sync, re-verification, and tombstone-cleanup jobs.</li><li>Certificate provisioning, renewal, and health-check jobs also require a wired <code>CertManager</code>.</li><li>Without those background services, the admin API can store domain bindings, but the automated verification/certificate/routing lifecycle described below does not run.</li></ul><h2 id="admin-api" tabindex="-1">Admin API <a class="header-anchor" href="#admin-api" aria-label="Permalink to &quot;Admin API&quot;">​</a></h2><p>Admin-token routes:</p><ul><li><code>GET /api/admin/domains</code></li><li><code>POST /api/admin/domains</code></li><li><code>GET /api/admin/domains/{id}</code></li><li><code>DELETE /api/admin/domains/{id}</code></li><li><code>POST /api/admin/domains/{id}/verify</code></li></ul><h3 id="create-domain" tabindex="-1">Create domain <a class="header-anchor" href="#create-domain" aria-label="Permalink to &quot;Create domain&quot;">​</a></h3><p>Request fields:</p><ul><li><code>hostname</code> (required)</li><li><code>environment</code> (optional; defaults to <code>production</code>)</li><li><code>redirectMode</code> (optional; allowed: <code>permanent</code>, <code>temporary</code>)</li></ul><p>Example:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -X</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> POST</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> http://localhost:8090/api/admin/domains</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -H</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;Authorization: Bearer &lt;admin-token&gt;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -H</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;Content-Type: application/json&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;hostname&quot;: &quot;app.example.com&quot;,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;environment&quot;: &quot;production&quot;,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;redirectMode&quot;: &quot;permanent&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  }&#39;</span></span></code></pre></div><p>Hostname validation enforced by <code>normalizeAndValidateHostname</code>:</p><ul><li>lowercases and trims input</li><li>rejects wildcards (<code>*</code>)</li><li>rejects IPv4/IPv6 literals</li><li>rejects hostnames with ports</li><li>requires at least two DNS labels</li><li>max total length 253, max label length 63</li><li>labels cannot start or end with <code>-</code></li><li>ASCII letters/digits/hyphens only</li></ul><h2 id="dns-verification" tabindex="-1">DNS verification <a class="header-anchor" href="#dns-verification" aria-label="Permalink to &quot;DNS verification&quot;">​</a></h2><p>On create, AYB generates a 64-char hex verification token and returns a computed challenge record:</p><ul><li><code>_ayb-challenge.&lt;hostname&gt; TXT &lt;token&gt;</code></li></ul><p>Example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_ayb-challenge.app.example.com TXT aabbccdd...</span></span></code></pre></div><p>Verification worker behavior (<code>domain_dns_verify</code>):</p><ul><li>polls TXT records at <code>_ayb-challenge.&lt;hostname&gt;</code></li><li>success transitions to <code>verified</code></li><li>retries while status is <code>pending_verification</code></li><li>timeout window is 24 hours from first attempt</li></ul><p>Retry cadence (<code>verifyRetryDelay</code>):</p><ul><li>attempts 1-20: 30-32 seconds</li><li>attempts 21+: 5m to 5m10s</li></ul><p>After timeout, AYB sets <code>verification_failed</code> with <code>lastError</code>.</p><h3 id="manual-re-trigger" tabindex="-1">Manual re-trigger <a class="header-anchor" href="#manual-re-trigger" aria-label="Permalink to &quot;Manual re-trigger&quot;">​</a></h3><p><code>POST /api/admin/domains/{id}/verify</code>:</p><ul><li>enqueues verification when status is <code>pending_verification</code> or <code>verification_failed</code></li><li>returns current binding unchanged for <code>verified</code>, <code>active</code>, and other non-verifiable states</li><li>rate limited per domain: 10 attempts/hour</li></ul><p>Rate-limit headers on verify endpoint:</p><ul><li><code>X-RateLimit-Limit</code></li><li><code>X-RateLimit-Remaining</code></li><li><code>X-RateLimit-Reset</code></li><li><code>Retry-After</code> (on <code>429</code>)</li></ul><h2 id="certificate-lifecycle" tabindex="-1">Certificate lifecycle <a class="header-anchor" href="#certificate-lifecycle" aria-label="Permalink to &quot;Certificate lifecycle&quot;">​</a></h2><p>Certificate jobs use certmagic-backed <code>CertManager</code>.</p><ul><li><code>verified</code> + no error enqueues <code>domain_cert_provision</code></li><li>provision success calls <code>SetDomainCert</code> and transitions to <code>active</code></li><li>active domain stores: <ul><li><code>certRef</code> (cert serial hex)</li><li><code>certExpiry</code></li></ul></li><li>provision failure keeps status <code>verified</code> and updates <code>lastError</code></li></ul><p>Renewal flow (<code>domain_cert_renew</code>):</p><ul><li>scans active domains with <code>cert_expiry &lt; now + 30 days</code></li><li>syncs refreshed cert metadata from cert cache</li><li>warns when cert is still within 7 days of expiry</li></ul><h2 id="health-and-re-verification-jobs" tabindex="-1">Health and re-verification jobs <a class="header-anchor" href="#health-and-re-verification-jobs" aria-label="Permalink to &quot;Health and re-verification jobs&quot;">​</a></h2><p>Health check (<code>domain_health_check</code>):</p><ul><li>runs on active domains with certs</li><li>sets <code>healthStatus</code> to <code>healthy</code> or <code>unhealthy</code></li><li>also warns when cert expiry is under 7 days</li></ul><p>DNS re-verification (<code>domain_reverify</code>):</p><ul><li>runs daily on active domains</li><li>increments <code>reverifyFailures</code> when challenge no longer matches</li><li>resets failures when DNS is valid again</li><li>after 3 consecutive failures, transitions to <code>verification_lapsed</code></li></ul><h2 id="routing-and-status-transitions" tabindex="-1">Routing and status transitions <a class="header-anchor" href="#routing-and-status-transitions" aria-label="Permalink to &quot;Routing and status transitions&quot;">​</a></h2><p>Route table includes only:</p><ul><li><code>active</code></li><li><code>verification_lapsed</code></li><li><code>tombstoned</code></li></ul><p>Middleware behavior:</p><ul><li>unknown host: request passes through</li><li><code>tombstoned</code> host: <code>421 Misdirected Request</code></li><li><code>active</code> and <code>verification_lapsed</code>: route entry added to request context</li></ul><p>Status values (<code>DomainStatus</code>):</p><ul><li><code>pending_verification</code></li><li><code>verified</code></li><li><code>active</code></li><li><code>verification_failed</code></li><li><code>verification_lapsed</code></li><li><code>tombstoned</code></li></ul><p>Cleanup windows from store queries/jobs:</p><ul><li><code>verification_lapsed</code> cleanup grace: 7 days (then tombstoned via reverify cleanup path)</li><li>tombstone reaping hard delete: 7 days after <code>tombstoned_at</code></li></ul><h2 id="schedules" tabindex="-1">Schedules <a class="header-anchor" href="#schedules" aria-label="Permalink to &quot;Schedules&quot;">​</a></h2><p>Registered cron schedules (UTC):</p><ul><li>route sync: every 5 minutes (<code>*/5 * * * *</code>)</li><li>health check: every 15 minutes (<code>*/15 * * * *</code>)</li><li>cert renew: every 12 hours (<code>0 */12 * * *</code>)</li><li>DNS reverify: daily at 04:00 (<code>0 4 * * *</code>)</li><li>tombstone reap: daily at 03:00 (<code>0 3 * * *</code>)</li></ul><h2 id="current-redirect-mode-limitation" tabindex="-1">Current redirect-mode limitation <a class="header-anchor" href="#current-redirect-mode-limitation" aria-label="Permalink to &quot;Current redirect-mode limitation&quot;">​</a></h2><p><code>redirectMode</code> is validated and stored (<code>permanent</code>/<code>temporary</code>) and carried in route entries, but there is no built-in HTTP redirect response path in <code>hostRouteMiddleware</code> yet. Treat it as routing metadata for now.</p><h2 id="related-guides" tabindex="-1">Related guides <a class="header-anchor" href="#related-guides" aria-label="Permalink to &quot;Related guides&quot;">​</a></h2><ul><li><a href="/guide/admin-dashboard.html#admin">Admin Dashboard</a></li><li><a href="/guide/security.html">Security</a></li><li><a href="/guide/deployment.html">Deployment</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/custom-domains.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const customDomains = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  customDomains as default
};
